---
# creating an ec2 instance 
 - name: creating instance
   hosts: local
   remote_user: root
   connection: ssh
   become: true
   become_method: sudo
   gather_facts: true
   tasks:
   - name: Creating ec2 instances
          
