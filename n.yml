---
 - name: Create an EC2 instance
   hosts: local
   remote_user: root
   become: false
   gather_facts: false
   connection: ssh
   tasks:
    - ec2:
        key_name: windowsnew
        instance_type: t2.micro
        image: ami-123456
        wait: yes
        group: webserver
        count: 1
        vpc_subnet_id: subnet-feec75b6
        assign_public_ip: yes
